import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: "export" — enable this for final static delivery to client
  // For now, running as standard Next.js on Vercel while design/copy/images are being finalized
};

export default nextConfig;
